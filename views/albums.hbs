<div class="container">
    <h1>Artists' albums</h1>
    {{#each albums}}
    <img src="{{images.0.url}}" alt="{{name}}"><br>
    {{name}}<br>
    <a href="/albums/preview/{{id}}">View Tracks</a>
    {{/each}}
</div>