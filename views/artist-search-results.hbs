<div class="container">
    <div class="row">
        {{#each datos}}
        <div class="col-4">
            <h1>{{name}}</h1>
            <img src="{{images.1.url}}" alt="{{name}}">
            <a href="/albums/{{id}}">View Albums</a>
        </div>
        {{/each}}
    </div>
</div>